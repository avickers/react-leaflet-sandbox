{
  "name": "census-map",
  "version": "2.0.2",
  "description": "Sandbox for Census Map",
  "main": "src/index.js",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "parcel build src/index.html",
    "start": "parcel src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "avickers",
  "license": "MIT",
  "dependencies": {
    "@avickers/knockdown": "^0.2.1",
    "leaflet": "^1.6.0",
    "lokijs": "^1.5.8"
  },
  "browserslist": [
    "last 3 chrome versions"
  ],
  "devDependencies": {
    "cssnano": "^4.1.10",
    "stats-lite": "^2.2.0"
  }
}
